name: darpan
description: Gender promotion equality detector
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6

  # UI & Charts
  fl_chart: ^0.65.0
  google_fonts: ^6.1.0

  # State Management
  provider: ^6.1.1

  # Utilities
  uuid: ^4.2.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true